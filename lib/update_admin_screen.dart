import 'package:flutter/material.dart';
class UpdateAdminScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Update Admin')),
      body: const Center(child: Text('Assign or remove Admins for teams here.')),
    );
  }
}

