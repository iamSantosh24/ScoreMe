import 'package:flutter/material.dart';
class UpdateSuperAdminScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Update Super Admin')),
      body: const Center(child: Text('Assign or remove Super Admins for leagues here.')),
    );
  }
}

